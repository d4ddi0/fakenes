

MAKE = @MAKE@

RM = @RM@ -f -v


all :

	cd src && ${MAKE} all


install :

	cd src && ${MAKE} install


uninstall :

	cd src && ${MAKE} uninstall


clean :

	cd src && ${MAKE} clean


distclean : clean

	${RM} config.log config.cache config.status

	${RM} Makefile


	cd src && ${MAKE} distclean
